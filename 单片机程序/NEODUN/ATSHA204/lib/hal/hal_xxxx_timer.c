
//#include "xxxx.h"

/*&&&&&&&&&&&&&&&&&&&         延时实现函数      &&&&&&&&&&&&&&&&&&&&&&&&&&&*/

void atca_delay_ms(uint16_t delay)
{	
	////////////////////////////////////////////////////////////////////////////////////////////
	//实现细节：
	/*
	参数delay表示需要延时的毫秒mS值，根据目标平台按此延时相应的毫秒值即可。
	*/
	////////////////////////////////////////////////////////////////////////////////////////////
}

